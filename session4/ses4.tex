\chapter{Functions}
% as a Refactor of Session 3 (Session 4)

\section{Introduction}

\begin{tcolorbox}[title=Goal]
\textbf{Refactor} sequential code into reusable functions, load a CSV dataset, and preserve the classifier rule from Session 3. This builds a clean foundation for upcoming sessions.
\end{tcolorbox}

\begin{tcolorbox}[title=You will work in]
Use the following files for this session:
\begin{itemize}
    \item \texttt{session4/session4\_iris\_template.py} (student template)
    \item \texttt{session4/iris\_data.csv} (dataset)
\end{itemize}
\textbf{How to run the file:} Use the terminal command: \\
\texttt{python3 session4/session4\_iris\_template.py} \\
\textbf{Do not edit other files.} We will reuse this file in later sessions.
\end{tcolorbox}

This session is all about how functions can dramatically improve the way you structure your code, making it more organized and reusable. Think of \textbf{functions} as essential building blocks for any programmer. They allow you to break down complex projects into smaller, more manageable, and importantly, reusable parts. By learning to effectively use functions, you'll be writing Python code that is not only cleaner and more efficient but also significantly easier to understand and maintain.

In Session 3, you built a solid foundation by learning about lists, loops for repetitive tasks, and conditional statements for decision-making. Now, we're going to elevate your Python skills further. This session, we'll focus on structuring your Python scripts more effectively using functions. We'll revisit code from a previous tutorial that was written sequentially and transform it by incorporating functions.

\section{What You Will Learn}
\textbf{In this session, you will learn to:}
\begin{enumerate}\setlength{\itemsep}{5pt}
\item \textbf{Refactor Code into Functions}: Take existing, sequential scripts and reorganize them using functions. This will teach you how to identify blocks of code that can be encapsulated into reusable functions.
\item \textbf{Understand Functions}: Learn the fundamental concept of functions -- what they are, how they are structured in Python, and why they are so beneficial for writing efficient and organized code.
\item \textbf{Load a Dataset from CSV}: In real projects, datasets can come from many file formats. For now, we use \texttt{.csv} because it is simple, human-readable, and supported by Python without extra libraries.
\end{enumerate}

\subsection{Functions Primer}
A \textbf{function} is a named block of code designed to perform one specific job. Instead of writing the same lines repeatedly, you define the logic once and \textit{call} the function whenever you need it. Functions take \textit{arguments} (inputs), perform actions, and \textit{return} a value (output).

\begin{minted}[fontsize=\small, linenos, frame=lines]{python}
def greet_student(name):
    # This function takes a name and returns a greeting
    return "Hello, " + name

print(greet_student("Alice"))
\end{minted}

\subsection{What is the common rule for creating functions?}
There are several rules of thumb for creating functions, but a key one is: \textbf{Each function should do one thing and do it well.} This means that a function should have a single responsibility or task. If you find yourself writing a function that is trying to accomplish multiple tasks, it's often a sign that you should break it down into smaller, more focused functions. This makes your code easier to read, test, and maintain. For example, if you have a function that both processes data and prints results, consider splitting it into two functions: one for processing and another for printing.

\subsection{The Benefit of function}
Using functions has several benefits:
\begin{itemize}
    \item \textbf{Reusability}: Once a function is defined, you can call it multiple times throughout your code without having to rewrite the same logic.
    \item \textbf{Organization}: Functions help break down complex problems into smaller, more manageable pieces, making your code easier to read and understand.
    \item \textbf{Maintainability}: If you need to change the logic, you only need to update it in one place (the function definition) rather than in every place where the logic is used.
    \item \textbf{Testing}: Functions can be tested independently, which makes it easier to identify and fix bugs.
\end{itemize}


\section{Session 3 to Session 4 Refactor Map}
Session 3 sequential blocks become these functions in Session 4:

\begin{itemize}
    \item session 3 rule block $\rightarrow$ \texttt{determine\_binary\_label(sample, settings)}
    \item session 3 truth conversion block $\rightarrow$ \texttt{determine\_true\_binary\_label(sample, settings)}
    \item session 3 metric update block $\rightarrow$ \texttt{update\_metrics(metrics, y\_pred, y\_true)}
    \item session 3 compare prediction to truth $\rightarrow$ \texttt{evaluate\_prediction(y\_pred, y\_true)}
    \item session 3 per-sample classifier step $\rightarrow$ \texttt{classify\_sample(sample, settings)}
    \item session 3 full loop and counters $\rightarrow$ \texttt{initialize\_predictions(dataset, settings)}
    \item session 3 top-level script setup $\rightarrow$ \texttt{setup\_application(filepath)} and \texttt{main()}
\end{itemize}

\section{Canonical Function Set}
Complete these functions in \texttt{session4\_iris\_template.py}. The table below shows the mapping:

\input{session4/tab4.tex}

\begin{tcolorbox}[title=Good Practice]
We deliberately keep the instruction verbose in this session. When students refactor code into functions, explicit guidance about inputs, outputs, and return values helps prevent structure mistakes.
\end{tcolorbox}
\section{Task 1 and Task 2: Setup Functions}

\begin{tcolorbox}[title=Task 1: Create a helper function to print status updates]
Replace \texttt{pass} inside \texttt{make\_print\_status(status\_text)} with the print statement below.
\end{tcolorbox}

\begin{minted}[fontsize=\small, linenos, breaklines=true, frame=lines]{python}
print(f"[STATUS] {status_text}")
\end{minted}

This helper function is used in \texttt{main()} to show what the program is doing.

\begin{tcolorbox}[title=Checkpoint]
After completing the Task 1, when executing the code, the compiler will the \texttt{make\_print\_status("Build dataset")}, and in terminal, you should see a printed output of \:
\texttt{[STATUS] Build dataset}
\end{tcolorbox}

\begin{tcolorbox}[title=Task 2: Create the flower dataset]

Inside \texttt{setup\_application\_list()}, complete the dataset setup so that the function builds the same flower dictionaries from Session 3 and returns the dataset list.
\end{tcolorbox}

The template already gives you \texttt{flower1}. Add \texttt{flower2} using the same canonical keys:

\begin{minted}[fontsize=\small, linenos, breaklines=true, frame=lines]{python}
flower2 = {
    "id": "flower2",
    "sepal_length": 4.9,
    "sepal_width": 3.0,
    "petal_length": 1.4,
    "petal_width": 0.2,
    "species": "setosa"
}
\end{minted}

Also,  uncomment the line with \texttt{flower2} in the dataset list, so the dataset line currently remains:

\begin{minted}[fontsize=\small, linenos, breaklines=true, frame=lines]{python}
dataset = [flower1,
           flower2
           ]
\end{minted}

\begin{tcolorbox}[title=Good Practice]
Keep the keys exactly the same as Session 3: \texttt{id}, \texttt{sepal\_length}, \texttt{sepal\_width}, \texttt{petal\_length}, \texttt{petal\_width}, and \texttt{species}. Refactoring should not invent new key names.
\end{tcolorbox}

\begin{tcolorbox}[title=Checkpoint]
\texttt{setup\_application\_list()} must finish with \texttt{return dataset}. If you forget the return statement, the dataset in \texttt{main()} will become \texttt{None}.
\end{tcolorbox}

\section{Main Flow Before the Loop}

\begin{tcolorbox}[title=Task 3 : Show a status update before running prediction]
In \texttt{main()}, keep the status call before the prediction loop:
\end{tcolorbox}

\begin{minted}[fontsize=\small, linenos, breaklines=true, frame=lines]{python}
make_print_status("Run prediction loop")
\end{minted}

This line is already present in the template, but students should understand that it depends on Task 1 working correctly.

\begin{tcolorbox}[title={Entry to Task 4 until Task 9: Run the same prediction loop from Session 3, Wrap the prediction workflow in a function}]
The function \texttt{run\_prediction\_loop(dataset)} becomes the new home for the Session 3 loop. You will keep the same beginner-friendly variables: \texttt{correct}, \texttt{wrong}, \texttt{total}, and \texttt{y\_pred\_list}.
\end{tcolorbox}

At the start of \texttt{run\_prediction\_loop(dataset)}, keep these initial values:

\begin{minted}[fontsize=\small, linenos, breaklines=true, frame=lines]{python}
correct = 0
wrong = 0
total = 0
y_pred_list = []
\end{minted}

\begin{tcolorbox}[title=Good Practice]
This function should \textbf{run the loop and return results}. It should not calculate the final accuracy. That job belongs to the accuracy function later.
\end{tcolorbox}

\section{Prediction Functions}

\begin{tcolorbox}[title=Task 5: Predict the class using petal length]
Replace \texttt{pass} inside \texttt{compute\_threshold\_prediction(sample)} with the same Session 3 rule and return the predicted label.
\end{tcolorbox}

\begin{minted}[fontsize=\small, linenos, breaklines=true, frame=lines]{python}
if sample[FEATURE_NAME] < THRESHOLD:
    y_pred = POSITIVE_LABEL
else:
    y_pred = NEGATIVE_LABEL

return y_pred
\end{minted}

\begin{tcolorbox}[title=Checkpoint]
This function must return either \texttt{POSITIVE\_LABEL} or \texttt{NEGATIVE\_LABEL}. If you forget \texttt{return y\_pred}, later code will receive \texttt{None}.
\end{tcolorbox}

\begin{tcolorbox}[title=Task 6: Convert species into the lesson’s binary label]
Inside \texttt{run\_prediction\_loop(dataset)}, add the function call that converts the real species into the binary lesson label.
\end{tcolorbox}

\begin{minted}[fontsize=\small, linenos, breaklines=true, frame=lines]{python}
y_true = derive_true_label(sample)
\end{minted}

The body of \texttt{derive\_true\_label(sample)} is already provided in the template:

\begin{minted}[fontsize=\small, linenos, breaklines=true, frame=lines]{python}
if sample[LABEL_KEY] == POSITIVE_LABEL:
    y_true = POSITIVE_LABEL
else:
    y_true = NEGATIVE_LABEL

return y_true
\end{minted}

\begin{tcolorbox}[title=Good Practice]
Notice the separation of responsibilities:
\begin{itemize}
    \item \texttt{compute\_threshold\_prediction(sample)} predicts from the feature value,
    \item \texttt{derive\_true\_label(sample)} converts the real species into the lesson's two-label format.
\end{itemize}
\end{tcolorbox}

In session 3, you updated counters and stored predictions.
In Session 4, refactor that logic into a function:

\begin{tcolorbox}[title=Task 7: Update prediction counts and save results]
Replace \texttt{pass} inside \texttt{update\_result\_counts(...)} with the Session 3 metric-update logic and return all updated values.
\end{tcolorbox}

\begin{minted}[fontsize=\small, linenos, breaklines=true, frame=lines]{python}
if y_pred == y_true:
    correct += 1
else:
    wrong += 1

total += 1
y_pred_list.append(y_pred)

return correct, wrong, total, y_pred_list
\end{minted}

\begin{tcolorbox}[title=Checkpoint]
\texttt{total} must increase for every processed sample, even when the prediction is wrong. The function must also return all four updated objects in the same order.
Use the debug modes in your IDE to step through the code and verify that the counters update correctly for both correct and wrong predictions.
\end{tcolorbox}

\section{Inside the Prediction Loop}

Inside \texttt{run\_prediction\_loop(dataset)}, the order of work should now be:
\begin{enumerate}
    \item print the current sample id,
    \item compute \texttt{y\_pred},
    \item derive \texttt{y\_true},
    \item update the counters and prediction list.
\end{enumerate}

\begin{minted}[fontsize=\small, linenos, breaklines=true, frame=lines]{python}
for sample in dataset:
    print("\nProcessing sample with id:", sample["id"])

    y_pred = compute_threshold_prediction(sample)
    y_true = derive_true_label(sample)
    correct, wrong, total, y_pred_list = update_result_counts(
        correct, wrong, total, y_pred_list, y_pred, y_true
    )
\end{minted}

\begin{tcolorbox}[title=Task 8: Display the result for each sample]
Uncomment the per-sample trace print block inside \texttt{run\_prediction\_loop(dataset)}.
\end{tcolorbox}

\begin{minted}[fontsize=\small, linenos, breaklines=true, frame=lines]{python}
print(
    f"id={sample['id']} | true={y_true} | pred={y_pred} | "
    f"petal_length={sample['petal_length']}"
)
\end{minted}

\begin{tcolorbox}[title=Checkpoint]
The per-sample trace should still match Session 3 format exactly. This keeps the output compatible while the internal code structure changes.
\end{tcolorbox}

\begin{tcolorbox}[title=Task 9: Return the prediction loop results]
Replace the placeholders at the end of \texttt{run\_prediction\_loop(dataset)} with the correct return values.
\end{tcolorbox}

\begin{minted}[fontsize=\small, linenos, breaklines=true, frame=lines]{python}
return correct, wrong, total, y_pred_list
\end{minted}

\begin{tcolorbox}[title=Good Practice]
Return values must stay in the same order as the variables received in \texttt{main()}. If the order changes, the wrong values will be assigned without an obvious syntax error.
\end{tcolorbox}

\section{Accuracy and Summary}

\begin{tcolorbox}[title=Task 10: Compute the overall accuracy]
One \texttt{calculate\_accuracy(correct, total)} function in the template is missing the return statement. Add the missing line:
\end{tcolorbox}

\begin{minted}[fontsize=\small, linenos, breaklines=true, frame=lines]{python}
return accuracy
\end{minted}

\begin{tcolorbox}[title=Task 10: Calculate accuracy]
In \texttt{main()}, keep the function call that computes the accuracy after the loop finishes:
\end{tcolorbox}

\begin{minted}[fontsize=\small, linenos, breaklines=true, frame=lines]{python}
accuracy = calculate_accuracy(correct, total)
\end{minted}

\begin{tcolorbox}[title=Checkpoint]
If \texttt{calculate\_accuracy(...)} does not return a value, \texttt{accuracy} in \texttt{main()} will become \texttt{None}.
\end{tcolorbox}

\begin{tcolorbox}[title=Task 11: Show a status update before printing the summary]
Replace the placeholder in \texttt{main()} with the exact status text from the answer file.
\end{tcolorbox}

\begin{minted}[fontsize=\small, linenos, breaklines=true, frame=lines]{python}
make_print_status("Print summary")
\end{minted}

\begin{tcolorbox}[title=Task 12: Create and call the summary-report function]
Call the summary function at the end of \texttt{main()}:
\end{tcolorbox}

\begin{minted}[fontsize=\small, linenos, breaklines=true, frame=lines]{python}
print_summary(correct, wrong, total, y_pred_list, accuracy)
\end{minted}

The provided \texttt{print\_summary(...)} function already prints:
\begin{minted}[fontsize=\small, linenos, breaklines=true, frame=lines]{python}
print("\n=== session 4 Summary ===")
print("Correct:", correct)
print("Wrong:", wrong)
print("Total:", total)
print("Accuracy (%):", round(accuracy, 2))
print("All predictions:", y_pred_list)
\end{minted}

\begin{tcolorbox}[title=Checkpoint]
By the end of \texttt{main()}, the program should:
\begin{itemize}
    \item print a status for building the dataset,
    \item print a status for running the prediction loop,
    \item run the loop and collect results,
    \item calculate the accuracy,
    \item print a status for the summary,
    \item print the final summary report.
\end{itemize}
\end{tcolorbox}

\section{About \texttt{\_\_main\_\_}}
At the bottom of the file, you will see:

\begin{minted}[fontsize=\small, linenos, breaklines=true, frame=lines]{python}
if __name__ == "__main__":
    main()
\end{minted}

This means:
\noindent
\textbf{Meaning:}
\begin{itemize}
    \item When you run the file directly, Python sets \texttt{\_\_name\_\_} to \texttt{"\_\_main\_\_"} and \texttt{main()} executes.
    \item When the file is imported by another file, \texttt{main()} does not automatically run.
\end{itemize}


\begin{tcolorbox}[title=Good Practice]
\texttt{main()} should read like a short plan of the whole program. The detailed work should stay inside helper functions.
\end{tcolorbox}

\section{Summary and Review}
\begin{itemize}
    \item \textbf{Task 1 to Task 3} prepare the program flow.
    \item \textbf{Task 5 to Task 9} move the Session 3 prediction loop into helper functions.
    \item \textbf{Task 10 to Task 12} compute accuracy and print the final summary.
\end{itemize}

\textbf{Reflection Questions:}
\begin{enumerate}
    \item Why is \texttt{return} essential in functions such as \texttt{compute\_threshold\_prediction(...)} and \texttt{calculate\_accuracy(...)}?
    \item Why is it useful to move the metric-update logic into \texttt{update\_result\_counts(...)}?
    \item How does \texttt{main()} become easier to read after refactoring the Session 3 code into functions?
\end{enumerate}

\textbf{Micro-Exercise:} Uncomment \texttt{flower2} inside the dataset list, run the program again, and observe how the loop results and summary update automatically.
